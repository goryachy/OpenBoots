services:
  openboots:
    build: .
    environment:
      CORE_MODE: demo
      DATABASE_PATH: /app/data/openboots.db
      JWT_SECRET: change-me-in-production
      PORT: 3001
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
